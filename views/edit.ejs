<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container py-5">
  <h1 class="mb-4">✏️ タスクを編集</h1>

  <form action="/edit/<%= task.id %>" method="POST">
  <input type="hidden" name="redirect" value="<%= redirectTo %>"> <!-- ← 追加 -->

  <div class="mb-3">
    <label for="content" class="form-label">タスク内容</label>
    <input type="text" name="content" id="content" class="form-control" value="<%= task.content %>" required>
  </div>

  <div class="mb-3">
    <label for="date" class="form-label">日付（任意）</label>
    <input type="date" name="date" id="date" class="form-control" value="<%= task.date ? task.date.toISOString().split('T')[0] : '' %>">
  </div>

  <button type="submit" class="btn btn-primary">更新</button>
</form>
</body>
</html>